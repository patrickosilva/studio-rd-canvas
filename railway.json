{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules/.cache node_modules && npm ci --include=dev && npm run db:generate && npm run build"
  },
  "deploy": {
    "startCommand": "npm run db:deploy && npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}