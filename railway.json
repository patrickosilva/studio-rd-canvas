{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g npm@12.0.1 && npm ci --allow-remote=all && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "npm run db:deploy && npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}